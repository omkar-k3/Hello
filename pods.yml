apiVersion: v1
kind: Pod
metadata:
  name: firstapp
  labels:
    app: pystritch
spec:
  containers:
    - name: pystritch
      image: omkarkhare/pystritch:13
      ports:
        - containerPort: 8080
